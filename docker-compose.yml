services:
  discorddeckbuilder:
    image: ghcr.io/sachskaylee/discorddeckbuilder:master
    volumes:
       - /root/containers/discorddeckbuilder/env:/discorddeckbuilder/.env.local
       - /root/containers/discorddeckbuilder/data:/discorddeckbuilder/data
    restart: unless-stopped
